# macOS 附加功能清理验收 · 2026-09-21

最终安装版本：`4.71.2-debug.09212325`。源码提交：`fd1c8dd`（附加功能删除）、
`3c14033`（任务树筛选修复）、`62fb23b`（不可达项目树删除）。

## 清理范围

- 删除侧栏「工具与帮助」、网页工具目录、完整 Web 控制台包装层，以及对应导航和返回桥接。
- 删除原生并行任务/收件箱、入门教程、独立快捷键说明页及连接页的冗长入门说明。
- 同步移除系统菜单、命令面板、设置、侧栏和任务右键菜单中的这些入口。
- 删除未渲染的旧项目树及其专用展开状态、项目弹窗和不可达 Worktree 审查视图。
- 两个纯清理提交合计删除 2,810 行、增加 19 行必要语法收口，净删 2,791 行。
  主仓库另外删除两条要求旧完整 Web 返回桥接的过时断言。
- 保留核心会话、工作任务、任务看板、PTY、文件/Git 检查器、实际操作快捷键和客户端更新。
  服务端 API、Web 产品和用户数据没有随此次原生清理删除。

任务侧栏原先只在未渲染的旧树中使用筛选词，实际任务树始终展示全部分组。
现已在当前数据路径筛选项目、目录、任务和会话；搜索时展开命中内容，清除后恢复原折叠状态。
查询变化清空多选，无结果显示明确提示，任务原始元信息和会话总数不被筛选改写。

## 自动验证

- `npm run check`：通过。
- `npm test`：最终运行 996/996 通过（工作区其他并行改动新增测试使数量增加）。
- macOS 完整 XCTest：51/51 通过，包含新增的 4 项任务树筛选回归。
- Universal Release 构建：成功，`x86_64 arm64`；安装包与应用版本一致。
- 安装后 `codesign --verify --strict`：通过；DMG `hdiutil verify`：通过。
- `git diff --check`：通过。
- DESIGN.md lint：0 errors、0 warnings；Premium strict 静态审计：0 findings。
  此静态工具对原生 SwiftUI 覆盖有限，不能替代真机验证。

## 真机与实际服务

整个原生 UI 验收使用用户指定连接码对应的真实 Wand 服务，不创建测试任务、不发送 AI 提示词，
不修改已有任务或会话。连接码保留在本机私密文件，未写入仓库或截图。

已完成的真机检查：

- 清理前实际打开工具菜单和 Web 包装页，确认其重复入口；清理后整块菜单消失。
- 首页保留标题、输入区和底部工具/模型/目录/任务按钮。
- 设置只显示通用、连接与服务、权限、故障排查、关于，最终安装版再次核对通过。
- 命令面板搜索「使用入门」为 0 结果；系统「显示→前往」只剩会话、工作空间和任务看板。
- 真实任务看板加载成功；任务右键菜单没有原生并行任务/收件箱入口。

验收途中指定 HTTPS 入口开始即时拒绝连接，之后重复请求仍为 `ECONNREFUSED`。
本机同一安装服务仍监听配置中的 8181 端口，目标已有任务详情经只读 GET 返回 200，
因此终端打开失败发生在网络连接阶段，尚未进入 WebView。
用户尚未确认临时切换客户端连接地址；最终安装版保留原地址，显示明确的连接失败和重试入口。
**最终版本的真实任务筛选、任务加号带入及 PTY 打开复查尚未完成，不能声称端到端验收全部通过。**

分发验证单独通过同一本机已安装服务的配置端口进行只读检查：
`/api/macos-dmg-update?currentVersion=0.0.0` 返回 200，
`latestVersion=4.71.2-debug.09212325`，`fileName=wand-v4.71.2-debug.09212325.dmg`，`source=local`。
这不替代原指定 HTTPS 地址的最终界面验收。

## 截图

- [首页清理概览](desktop-cleanup-overview.jpg)：清理中间包 `4.71.2-debug.09212307`，
  已删除附加工具，捕获于网络故障后的缓存首页；不作为最终筛选结果或当前在线状态的证明。
- [最终版设置](settings-cleanup.jpg)：最终安装包 `4.71.2-debug.09212325`。

## 分发文件

| 文件 | 字节数 | SHA-256 |
| --- | ---: | --- |
| wand-v4.71.2-debug.09212325.dmg | 10122923 | 2d36b2200560276057e968f7a323f0445e64981d7d6749069de90daa6e18815c |
| wand-v4.71.2-debug.09212325.zip | 7765574 | 0815536cd4a7061293e17dfd1f74cae852e6af60962ff28a5941f17cc102e712 |
| wand-v4.71.2-debug.09212325.update.json | 396 | 75deeb4263bd072cfa885d1872f15e18ef0338aace10e4fbdefc326509a9b95d |
