# 连续侧栏验收 · 2026-09-21

最终版本：`4.71.2-debug.09212341`，已安装到本机 Wand，并放入已安装服务的 macOS 更新目录。

## 行为

- 删除工作空间/会话切换按钮与旧会话/目录模式，废弃相应模式偏好。
- 主壳拥有唯一的侧栏 ScrollView：工作空间、任务、任务会话在上，单独会话与旧服务器可恢复历史在下。
- 工作空间目录默认收起，单目录也可收起；选择任务或任务内会话展开对应路径。
  搜索临时展开结果，清除搜索恢复原展开集合；后台刷新不重置手动收起状态。
- 上下共用筛选。下方归属按未筛选的完整任务数据判定，不按 workspaceId 误排普通会话；
  支持悬空绑定、陈旧快照、截断子会话和任务接口失败回退。
- 两个分区拥有局部加载、失败、重试和空态；不会用整栏空态遮住另一部分。
- 下方刷新不再抢走上方任务选择；多选会随筛选和可见归属变化清理，确认时再次检查有效范围。
- 工作空间标题提供新任务入口；目录/任务加号继续复用首页的默认值与上下文。
  ⌘2 / ⌘1 定位上方 / 下方分区，保留当前主区任务或会话。
- 删除旧模式切换、侧栏 section 通知、目录模式专用递归视图及上方独立会话渲染路径。
  本轮源文件与测试删除 655 行、新增 517 行，净减少 138 行（不含设计文档及证据）。

## 验证

- macOS 完整 XCTest：56/56 通过；其中 WorkspaceTaskContractTests 19/19，新增 5 项会话归属回归。
- `npm run check`：通过。`npm test`：1000/1000 通过。
- Universal Release 构建成功，`x86_64 arm64`；最终包另统一了两个分区的重试按钮样式。
- 最终安装版本和包版本一致；`codesign --verify --strict`、`hdiutil verify`、`git diff --check` 通过。
- DESIGN.md lint：0 errors / 0 warnings；Premium strict 静态审计：0 findings。
  静态审计不证明原生交互或无障碍全部通过。

## 真机证据与限制

始终保留用户指定连接码对应的 HTTPS 地址，没有切换到隔离服务或另建模拟服务。
地址持续返回连接拒绝（`ECONNREFUSED`），因此本轮真实数据内容、目录展开/搜索去重、任务加号预填与终端打开
**尚未完成端到端真机验收**；测试数据不能代替该验收。

已实际操作：

- 最终安装版 AX 树只有一个侧栏滚动区域，其内部依次显示工作空间、新任务、单独会话。
- 旧切换按钮与目录模式入口消失。
- 在统一筛选框输入文字，清除按钮出现；点击后文本清空、焦点回到输入框。
- 隐藏侧栏后使用 ⌘2，恢复整个连续侧栏；⌘1 不切换或隐藏另一分区。
- 失败状态下，两部分的标题、局部重试和全局连接恢复入口都可达。
- 验收后清空筛选，保持侧栏可见，连接地址不变。

[最终版离线布局截图](sidebar-offline.jpg) 如实记录该网络故障状态，不是成功加载会话数据的证明。
本轮未重新进行最小窗口、中文输入法或完整 VoiceOver 验证。

分发文件通过同一本机已安装服务配置端口做只读校验：
`/api/macos-dmg-update?currentVersion=0.0.0` 返回 200，`latestVersion=4.71.2-debug.09212341`，
`fileName=wand-v4.71.2-debug.09212341.dmg`，`source=local`。该分发检查不替代指定 HTTPS 的界面验收。

## 产物

| 文件 | 字节 | SHA-256 |
| --- | ---: | --- |
| wand-v4.71.2-debug.09212341.dmg | 9968690 | ac238f23018837c0ab29b5c5b3d43c5db6c7d237c08cdb7dc214c9bdeb40cb6b |
| wand-v4.71.2-debug.09212341.zip | 7642563 | 308b1ef57a9c7474c225916a0c4a90fd83c7161502fd7c391c3bf1cde79c1dc3 |
| wand-v4.71.2-debug.09212341.update.json | 395 | d052569f91b80b42d084f6435d7780fa94a1f5a4449fb1a1ded3c0456cd6768e |
